<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bg71ul.assignment">

    <permission
        android:name="com.example.bg71ul.assignment.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.example.bg71ul.assignment.WRITE_DATABASE"
        android:protectionLevel="normal" />

     <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
     <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainMenu" />
        <activity android:name=".activities.CurrencyActivity" />
        <activity
            android:name=".activities.GalleryActivity"
            android:label="@string/title_activity_gallery"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name=".MuseumProvider"
            android:authorities="com.example.bg71ul.assignment"
            android:exported="true"
            android:readPermission="com.example.bg71ul.assignment.READ_DATABASE"
            android:writePermission="com.example.bg71ul.assignment.WRITE_DATABASE" />
    </application>

</manifest>